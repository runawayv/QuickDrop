<Window x:Class="QuickDrop.Views.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История — QuickDrop"
        Width="680" Height="520" MinWidth="560" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextPrimary}"
        FontFamily="Segoe UI" FontSize="13">

	<Window.Resources>
		<Style TargetType="GridViewColumnHeader">
			<Setter Property="Background" Value="{DynamicResource CardBackground}"/>
			<Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="GridViewColumnHeader">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource CardBorder}"
                                BorderThickness="0,0,1,1" Padding="10,8">
							<ContentPresenter VerticalAlignment="Center"/>
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Window.Resources>

	<DockPanel Margin="18">
		<Grid DockPanel.Dock="Top" Margin="0,0,0,14">
			<TextBlock Text="История передач" FontSize="18" FontWeight="Bold"/>
			<Button Content="Очистить историю" HorizontalAlignment="Right"
                    Style="{StaticResource GhostButton}" Padding="16,7" Click="OnClear"/>
		</Grid>

		<ListView x:Name="List" Background="Transparent" BorderThickness="0">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Файл" Width="240" DisplayMemberBinding="{Binding Name}"/>
					<GridViewColumn Header="Направление" Width="130" DisplayMemberBinding="{Binding DirectionText}"/>
					<GridViewColumn Header="Размер" Width="90" DisplayMemberBinding="{Binding SizeText}"/>
					<GridViewColumn Header="Дата" Width="130" DisplayMemberBinding="{Binding TimeText}"/>
					<GridViewColumn Header="Статус" Width="90" DisplayMemberBinding="{Binding Status}"/>
				</GridView>
			</ListView.View>
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
					<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
					<Setter Property="Template">
						<Setter.Value>
							<ControlTemplate TargetType="ListViewItem">
								<Border x:Name="bd" Background="Transparent" Padding="0,2">
									<GridViewRowPresenter Content="{TemplateBinding Content}"/>
								</Border>
								<ControlTemplate.Triggers>
									<Trigger Property="IsMouseOver" Value="True">
										<Setter TargetName="bd" Property="Background"
                                                Value="{DynamicResource HoverBackground}"/>
									</Trigger>
								</ControlTemplate.Triggers>
							</ControlTemplate>
						</Setter.Value>
					</Setter>
				</Style>
			</ListView.ItemContainerStyle>
		</ListView>
	</DockPanel>
</Window>